# Required
LINEAR_API_KEY=
CHARLIEHOOKS_SHARED_SECRET=

# Optional (defaults shown)
LINEAR_TEAM_KEY=CHA
HOST=127.0.0.1
PORT=8787

# Comma-separated host allowlist for `charlie-acceptance` http checks.
# Defaults to: stagswtf.github.io,.stags.wtf
ACCEPTANCE_ALLOWED_HOSTS=
